## Task

The following scene failed validation. Your task is to repair it.

### Original Scene Code
```python
{{ scene_file_content }}
```

### Failure Reason
```
{{ failure_reason }}
```

Review the code and the error, search for working examples in the collections, and provide a corrected version of the scene body.

Generate a JSON object matching the `SceneBuild` schema, containing the fixed `scene_body` and your `reasoning`.
