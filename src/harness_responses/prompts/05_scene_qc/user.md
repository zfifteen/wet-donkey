## Task

Review the following Manim scene and its rendered output.

**Scene Code:**
```python
{{ scene_code }}
```

**Rendered Video:**
(Assume a video player is available here to view the scene)

Identify any issues and provide a quality score. If there are no issues, return an empty list for `issues` and set `passed` to `true`.

Generate a JSON object matching the `SceneQC` schema.
